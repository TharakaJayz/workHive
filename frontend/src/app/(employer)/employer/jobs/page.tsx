

export default function EmployerJobsPage () {
    
    // job listing table - close posting | edit btn | view applicants
    // shadeCN dialog + form  using job edit
    // when view applicants clicked we need to call /api/v1/jobs/:id/applicants and show data in shadeCN dialog + table (accpect/reject) view resum will load resum in new tab
    return (

        <div>
            My posted jobs — list, edit, close posting

           
            
        </div>
    )
}