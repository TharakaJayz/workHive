# Root env example referencing per-service examples

# Backend (copy to backend/.env)
# See backend/.env.example for details

# Frontend (copy to frontend/.env)
# See frontend/.env.example for details
